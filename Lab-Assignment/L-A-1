#include<stdio.h>
int main()
{
    int n,i,sum=0;
    printf("Enter Range from 1 to :");
    scanf("%d",&n);
    for(i=1;i<=n;i=i+1)
    {
        sum=sum+i;
        
    }
    printf("The sum from 1 to %d : %d",n,sum);

}
